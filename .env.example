# Agente_Perfilamiento Environment Variables
# Copy this file to .env and fill in your actual values
#
# MULTIPLE .ENV FILES SUPPORT:
# This application supports loading multiple .env files with priority order:
# 1. .env.local (local overrides, highest priority, gitignored)
# 2. .env.{ENVIRONMENT} (environment-specific settings, e.g., .env.development, .env.production)
# 3. .env (base/default settings, lowest priority)
#
# Example usage:
# - Copy this file to .env for base settings
# - Create .env.development for development-specific overrides
# - Create .env.local for your personal local overrides (will be gitignored)
#
# The application will automatically load all existing files in priority order.

# LLM Configuration (Required)
LLM_API_KEY=your_llm_api_key_here
LLM_MODEL_NAME=gpt-4o-mini
LLM_PROVIDER=openai
LLM_BASE_URL=

# Other providers (when LLM_PROVIDER=custom)
# LLM_CUSTOM_ENDPOINT=https://your-custom-endpoint.com

# AWS Configuration (Optional - for advanced features)
AWS_REGION_NAME=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_SESSION_TOKEN=your_aws_session_token

# Application Configuration
ENVIRONMENT=development
LOG_LEVEL=INFO

# Data Storage Configuration
DATA_DIR=data
CONVERSATIONS_DIR=data/conversations
MEMORY_DIR=data/memory

# Agent Configuration
AGENT_TEMPERATURE=0.1
MAX_CONVERSATION_HISTORY=50
ENABLE_MEMORY_PERSISTENCE=true

# Optional: Database Configuration (if using database persistence)
# DATABASE_URL=opensearch+http://localhost:9200
# DATABASE_POOL_SIZE=5

# Optional: Redis Configuration (if using Redis for caching)
# REDIS_URL=redis://localhost:6379
# REDIS_TTL=3600